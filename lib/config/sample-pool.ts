/**
 * Fixed pool id until pool picker / auth exists.
 * Keep in sync with ashbracket/supabase/seed.sql.
 */
export const SAMPLE_POOL_ID = "a0000001-0000-4000-8000-000000000001" as const;

/** Default join code for the sample pool (seed + migration). */
export const SAMPLE_POOL_JOIN_CODE = "ASH2026" as const;
